FROM python:3.11-slim
WORKDIR /app
COPY . .
RUN python -m pip install --no-cache-dir -e ".[postgres,search]"
EXPOSE 8100
CMD ["uvicorn", "stock_content.api.main:create_app", "--factory", "--host", "0.0.0.0", "--port", "8100"]
