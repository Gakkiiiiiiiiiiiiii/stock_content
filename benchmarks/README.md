# Benchmarks

Benchmarks record latency, CPU, memory and (where applicable) GPU budgets
alongside the exact golden-set and contract versions. Performance changes must
preserve signal semantics or increment the policy version.
